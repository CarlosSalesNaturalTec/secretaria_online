/**
 * Arquivo: frontend/.env.example
 * Descrição: Template de variáveis de ambiente para o frontend React
 * Feature: feat-004 - Configurar variáveis de ambiente
 * Criado em: 2025-10-24
 *
 * IMPORTANTE:
 * - Este é um arquivo de exemplo. Copie para .env e preencha com valores reais.
 * - Variáveis de ambiente no Vite devem começar com VITE_ para serem expostas ao código
 * - NUNCA coloque dados sensíveis (senhas, chaves secretas) em variáveis do frontend!
 * - O frontend é executado no navegador, todas as variáveis são públicas
 */

# ==============================================================================
# CONFIGURAÇÕES DA API BACKEND
# ==============================================================================

# URL base da API backend
# Desenvolvimento: http://localhost:3000/api/v1
# Produção: https://seudominio.com/api/v1
# Staging: https://staging.seudominio.com/api/v1
#
# IMPORTANTE: Não inclua barra (/) no final da URL
VITE_API_BASE_URL=http://localhost:3000/api/v1

# Timeout de requisições HTTP (em milissegundos)
# 30 segundos = 30000ms
# Aumente se o servidor for lento ou para uploads grandes
VITE_API_TIMEOUT=30000

# ==============================================================================
# CONFIGURAÇÕES DE AMBIENTE
# ==============================================================================

# Ambiente de execução: 'development', 'production', 'staging'
# Usado para condicionar comportamentos (ex: logs, modo debug)
VITE_APP_ENV=development

# Nome da aplicação (usado em títulos, headers)
VITE_APP_NAME="Secretaria Online"

# Versão da aplicação (sincronize com package.json)
VITE_APP_VERSION=0.1.0

# ==============================================================================
# CONFIGURAÇÕES DE AUTENTICAÇÃO
# ==============================================================================

# Chave para armazenar token JWT no localStorage
# Padrão: 'auth_token'
# Não mude a menos que tenha um motivo específico
VITE_AUTH_TOKEN_KEY=auth_token

# Chave para armazenar dados do usuário no localStorage
# Padrão: 'auth_user'
VITE_AUTH_USER_KEY=auth_user

# Tempo de expiração do token em minutos (deve coincidir com backend)
# Usado para calcular quando renovar o token
# Padrão: 15 minutos
VITE_AUTH_TOKEN_EXPIRATION=15

# ==============================================================================
# CONFIGURAÇÕES DE UPLOAD DE ARQUIVOS
# ==============================================================================

# Tamanho máximo de arquivo para upload (em bytes)
# 10MB = 10485760 bytes
# Deve ser menor ou igual ao MAX_FILE_SIZE do backend
VITE_MAX_FILE_SIZE=10485760

# Tipos de arquivo aceitos (MIME types, separados por vírgula)
# Usado para validação no frontend antes de enviar ao servidor
VITE_ALLOWED_FILE_TYPES=application/pdf,image/jpeg,image/jpg,image/png

# Extensões de arquivo aceitas (para exibir ao usuário)
VITE_ALLOWED_FILE_EXTENSIONS=.pdf,.jpg,.jpeg,.png

# ==============================================================================
# CONFIGURAÇÕES DE PAGINAÇÃO E LISTAGENS
# ==============================================================================

# Número padrão de itens por página em tabelas
# Usado em listagens de alunos, professores, documentos, etc.
VITE_DEFAULT_PAGE_SIZE=20

# Opções de tamanho de página (separadas por vírgula)
# Mostradas em dropdowns de "Itens por página"
VITE_PAGE_SIZE_OPTIONS=10,20,50,100

# ==============================================================================
# CONFIGURAÇÕES DE UI/UX
# ==============================================================================

# Tempo de exibição de notificações toast (em milissegundos)
# 3 segundos = 3000ms
VITE_TOAST_DURATION=3000

# Posição das notificações toast
# Valores: 'top-right', 'top-left', 'bottom-right', 'bottom-left', 'top-center', 'bottom-center'
VITE_TOAST_POSITION=top-right

# Habilitar modo debug no console?
# true: Logs detalhados no console do navegador
# false: Logs mínimos (recomendado para produção)
VITE_ENABLE_DEBUG=true

# ==============================================================================
# CONFIGURAÇÕES DE FORMATO E LOCALIZAÇÃO
# ==============================================================================

# Localização padrão (para formatação de datas, números)
# pt-BR: Português do Brasil
# en-US: Inglês (Estados Unidos)
VITE_LOCALE=pt-BR

# Formato de data padrão
# dd/MM/yyyy: 24/10/2025 (padrão brasileiro)
# MM/dd/yyyy: 10/24/2025 (padrão americano)
VITE_DATE_FORMAT=dd/MM/yyyy

# Formato de data e hora padrão
# dd/MM/yyyy HH:mm: 24/10/2025 14:30
VITE_DATETIME_FORMAT=dd/MM/yyyy HH:mm

# Fuso horário
# America/Sao_Paulo: Horário de Brasília
VITE_TIMEZONE=America/Sao_Paulo

# ==============================================================================
# CONFIGURAÇÕES DE RECURSOS OPCIONAIS
# ==============================================================================

# Habilitar React Query Devtools?
# true: Ferramenta de debug para React Query visível
# false: Devtools desabilitadas (recomendado para produção)
VITE_ENABLE_REACT_QUERY_DEVTOOLS=true

# Tempo de cache padrão do React Query (em milissegundos)
# 5 minutos = 300000ms
# Dados em cache serão considerados "stale" após esse tempo
VITE_REACT_QUERY_STALE_TIME=300000

# Tempo de retenção em cache do React Query (em milissegundos)
# 10 minutos = 600000ms
# Dados em cache serão removidos após esse tempo de inatividade
VITE_REACT_QUERY_CACHE_TIME=600000

# ==============================================================================
# CONFIGURAÇÕES DE LINKS E RECURSOS EXTERNOS
# ==============================================================================

# URL da documentação da aplicação (se disponível)
VITE_DOCS_URL=https://docs.seudominio.com

# URL de suporte/helpdesk (se disponível)
VITE_SUPPORT_URL=https://suporte.seudominio.com

# Email de contato para suporte
VITE_SUPPORT_EMAIL=suporte@seudominio.com

# ==============================================================================
# INSTRUÇÕES DE USO
# ==============================================================================

# 1. Copie este arquivo para .env:
#    cp .env.example .env
#
# 2. Preencha as variáveis com valores reais (principalmente VITE_API_BASE_URL)
#
# 3. IMPORTANTE: Variáveis Vite devem começar com VITE_ para serem expostas ao código!
#
# 4. Após modificar .env, reinicie o servidor de desenvolvimento:
#    npm run dev
#
# 5. Acesse as variáveis no código com:
#    const apiUrl = import.meta.env.VITE_API_BASE_URL
#
# 6. NUNCA commite o arquivo .env no Git!
#    O .gitignore já está configurado para ignorá-lo
#
# 7. Para produção, use variáveis de ambiente do sistema de CI/CD ou servidor
#    Evite hardcoding de valores de produção neste arquivo
#
# ==============================================================================
